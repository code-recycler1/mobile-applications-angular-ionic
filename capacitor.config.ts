import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile-lecture2-example-start',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
